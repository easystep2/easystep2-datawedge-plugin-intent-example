import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'easystep2-datawedge-plugin-intent-example',
  webDir: 'www'
};

export default config;
